\* Configuration for buggy model (both fixes disabled)
\* Expected violations:
\*   BackendConsistency — advance and upsert paths diverge
\*   NoLLMSilenceAfterAdvance — LLM goes silent after session.update
\*   BackendRecordSync — upsert path falls behind advance path
\*   MonotonicProgress — UIRefetch can regress uiIndex

CONSTANTS
    MAX_Q = 4
    FIX_RECORD = FALSE
    FIX_REPROMPT = FALSE

SPECIFICATION Spec

INVARIANTS
    TypeInvariant
    ErrorConsistency
    BackendConsistency
    NoLLMSilenceAfterAdvance
    NoSilentDesync
    UINotBehindAdvance
    BackendRecordSync

PROPERTIES
    MonotonicProgress
    LLMEventualResponse
    BackendEventualSync
